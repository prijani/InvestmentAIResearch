# 📊 AI Stock Recommendation Tracker

**Purpose:**  
IN-PROGRESS. This repository tracks and verifies stock recommendations generated by AI over time. It records structured predictions for S&P 500 stocks, appends accurate historical prices, and saves everything with timestamps for reproducibility. 

> **Note:** The AI-based financial advisor project is separate and not included here.

---

## 🧩 Features

- Queries an OpenAI model for **top 10 S&P 500 stock recommendations** in JSON format.
- Records for each stock:
  - Ticker symbol
  - Company name
  - Recommendation (`Buy/Hold/Sell`)
  - Confidence score (0–1)
  - Short reasoning (1-2 sentences)
  - **Accurate end of month prior stock price** (`yfinance`)
  - Timestamp of when the recommendation was generated
- Saves structured JSON files in `outputs/`
- Maintains SHA256 hash log for data integrity verification

---

## ⚙️ Requirements

- Python 3.13+
- Packages:
  - `openai`
  - `python-dotenv`
  - `yfinance`

Install dependencies:

```bash
python -m pip install openai python-dotenv yfinance
